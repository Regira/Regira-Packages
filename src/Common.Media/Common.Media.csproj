<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFrameworks>net8.0;net10.0</TargetFrameworks>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <Authors>Regira bv</Authors>
    <RootNamespace>Regira.Media</RootNamespace>
    <PackageId>Regira.Media</PackageId>
    <AssemblyName>Regira.Media</AssemblyName>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <NoWarn>1701;1702;1591</NoWarn>
    <Version>6.0.1</Version>
    <Description>Abstractions for image and media processing in Regira.</Description>
    <PackageTags>regira;media;images;drawing</PackageTags>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\Common\Common.csproj" />
  </ItemGroup>
  <!-- MSBuild targets — auto-imported into consuming builds via NuGet buildTransitive -->
  <ItemGroup>
    <None Include="build\Regira.Media.props" Pack="true" PackagePath="buildTransitive\" />
    <None Include="build\Regira.Media.targets" Pack="true" PackagePath="buildTransitive\" />
  </ItemGroup>
  <!-- AI instruction files -->
  <ItemGroup>
    <None Include="ai\media.instructions.md" Pack="true" PackagePath="ai\" />
    <None Include="ai\media.examples.md" Pack="true" PackagePath="ai\" />
  </ItemGroup>
</Project>