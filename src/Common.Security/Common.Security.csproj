<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFrameworks>net8.0;net10.0</TargetFrameworks>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <Authors>Regira bv</Authors>
    <RootNamespace>Regira.Security</RootNamespace>
    <PackageId>Regira.Security</PackageId>
    <AssemblyName>Regira.Security</AssemblyName>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <NoWarn>1701;1702;1591</NoWarn>
    <Version>6.1.2</Version>
    <Description>Abstractions for security, hashing, and encryption in Regira.</Description>
    <PackageTags>regira;security;hashing;encryption</PackageTags>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\Common\Common.csproj" />
  </ItemGroup>
  <!-- MSBuild targets — auto-imported into consuming builds via NuGet buildTransitive -->
  <ItemGroup>
    <None Include="build\Regira.Security.props" Pack="true" PackagePath="buildTransitive\" />
    <None Include="build\Regira.Security.targets" Pack="true" PackagePath="buildTransitive\" />
  </ItemGroup>
  <!-- AI instruction files -->
  <ItemGroup>
    <None Include="ai\security.card.md" Pack="true" PackagePath="ai\" />
    <None Include="ai\security.instructions.md" Pack="true" PackagePath="ai\" />
    <None Include="ai\security.examples.md" Pack="true" PackagePath="ai\" />
  </ItemGroup>
</Project>