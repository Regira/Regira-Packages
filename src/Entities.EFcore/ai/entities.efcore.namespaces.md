# Regira.Entities.EFcore — Namespace Reference

> This package has no separate namespace list. Every `Regira.Entities.EFcore.*` namespace — services, query
> builders, processors, preppers, primers, normalizing, plus the commonly-missed `Regira.DAL.*` extensions —
> is documented in the `Regira.Entities` package, which is also where the setup, instructions, examples and
> signatures guides live.
>
> `get_package(id: "Regira.Entities", section: "namespaces")` — or `get_package_toc(id: "Regira.Entities")` for
> the full guide set.
